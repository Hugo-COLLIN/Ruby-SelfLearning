<header class="navbar">
    <nav>
        <%= link_to 'Mon blog', root_path, id: 'Un id de tag' %>
        <%= link_to 'Tous les posts', posts_path %>
        <% if user_signed_in? %>
            <%= link_to current_user.email, user_path(current_user), class: 'btn btn-info' %>
            <%# <%= link_to 'Déconnexion', destroy_user_session, method: :delete %>
        <% else %>
            <%= link_to 'Connexion', new_user_session_path %>
            <%= link_to 'Inscription', new_user_registration_path %>
        <% end %>
    </nav>
</header>